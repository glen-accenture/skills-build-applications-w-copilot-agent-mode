# OctoFit Tracker Backend

Node.js + Express + TypeScript backend for OctoFit Tracker.

Run:

```bash
npm install
npm run dev
```

The backend listens on port 8000 by default and connects to MongoDB on port 27017.
